# Copy to .env and fill in your key.
OPENAI_API_KEY=sk-...

# Optional overrides:
# SYNTH_MODEL=gpt-4o        # model for the user-facing cited answer
# LLM_MODEL=gpt-4o-mini     # cheap internal calls (decompose, self-query)
# RERANKER_ENABLED=1
